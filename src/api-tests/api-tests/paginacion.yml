info:
  name: paginacion
  type: http

http:
  method: GET
  url: http://localhost:8080/recetas?pagina=2
  params:
    - name: pagina
      value: "2"
      type: query
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
