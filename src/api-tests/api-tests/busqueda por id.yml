info:
  name: busqueda por id
  type: http

http:
  method: GET
  url: http://localhost:8080/receta?id=1
  params:
    - name: id
      value: "1"
      type: query
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
